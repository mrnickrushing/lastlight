{
  "name": "lastlight",
  "private": true,
  "version": "0.53.0",
  "description": "Source, validation, and production blueprint for the Roblox game Last Light.",
  "scripts": {
    "bootstrap": "node scripts/bootstrap.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/validate-plan.mjs && python3 scripts/validate_call_arity.py && python3 scripts/validate_mesh_assets.py && python3 scripts/validate_image_assets.py && python3 scripts/validate_audio_assets.py && python3 scripts/validate_monetization.py && python3 scripts/validate_schema_freeze.py && python3 scripts/validate_launch_checklist.py && python3 scripts/generate_release_docs.py --check && python3 scripts/validate_rollback_target.py && python3 scripts/validate_town_layout.py",
    "format": "stylua src tests scripts/verify-build.luau",
    "format:check": "stylua --check src tests scripts/verify-build.luau",
    "notes": "python3 scripts/generate_release_docs.py",
    "lint": "selene src tests scripts/verify-build.luau",
    "typecheck": "node scripts/typecheck.mjs",
    "test:luau": "lune run tests/run",
    "verify:build": "lune run scripts/verify-build",
    "verify:rollback": "node scripts/verify_rollback_build.mjs",
    "test": "npm run check && npm run format:check && npm run lint && npm run typecheck && npm run test:luau && npm run build && npm run verify:build && npm run verify:rollback",
    "publish": "python3 scripts/publish_place.py"
  },
  "engines": {
    "node": ">=20"
  }
}
